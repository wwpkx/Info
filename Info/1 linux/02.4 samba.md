# samba
```
Linux虚拟机安装Samba​​
sudo apt-get install samba  # Ubuntu/Debian
sudo systemctl restart smbd

配置共享目录​​
编辑/etc/samba/smb.conf，添加：
[shared]
path = /home/wwp/share
writable = yes
guest ok = no
valid users = wwp

创建Samba用户​​
sudo smbpasswd -a wwp
```
