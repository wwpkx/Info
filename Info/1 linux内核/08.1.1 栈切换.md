---
mindmap-plugin: basic
---

# 栈切换

## 用户栈→内核栈
- 前提，每个进程有独立的用户栈和内核栈
- CPU硬件，保存用户态上下文至内核栈
- Linux内核（SAVE_ALL），保存通用寄存器至内核栈
- 中断服务例程（ISR），使用内核栈存储局部变量、函数调用上下文
- Linux内核（RESTORE_ALL），恢复通用寄存器从内核栈
- CPU硬件（iret），恢复用户态上下文从内核栈
- 用户程序，使用用户栈继续执行用户程序

## 进程切换
- **进程切换时的内核栈切换**
- 进程切换 ==> 切换进程的内核栈